implicit-derivation-structural-record2-missing.mo:10.8-10.60: type error [M0230], Cannot determine implicit argument `compare` of type
  ({flag : Bool; n : Nat}, {flag : Bool; n : Nat}) -> Order.Order
note: Implicit derivation failed:
  `compare : (Bool, Bool) -> Order` — not found
  `compare : (Nat, Nat) -> Order` — not found
