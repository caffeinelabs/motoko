implicit-derivation-deep.mo:20.8-20.46: type error [M0230], Cannot determine implicit argument `compare` of type
  ([Bool], [Bool]) -> Order
note: If you're trying to omit an implicit argument named `compare` you need to have a matching declaration named `compare` in scope.
note: Implicit derivation attempted:
  via `Array.compare`, `compare` : `(Bool, Bool) -> Order` — try importing mo:core/Bool
implicit-derivation-deep.mo:28.8-28.56: type error [M0230], Cannot determine implicit argument `compare` of type
  ([[Bool]], [[Bool]]) -> Order
note: If you're trying to omit an implicit argument named `compare` you need to have a matching declaration named `compare` in scope.
note: Implicit derivation attempted:
  via `Array.compare`, `compare` : `([Bool], [Bool]) -> Order`
    via `Array.compare`, `compare` : `(Bool, Bool) -> Order` — try importing mo:core/Bool
implicit-derivation-deep.mo:38.8-38.55: type error [M0230], Cannot determine implicit argument `compare` of type
  (([Color], [Int]), ([Color], [Int])) -> Order
note: If you're trying to omit an implicit argument named `compare` you need to have a matching declaration named `compare` in scope.
note: Implicit derivation attempted:
  via `Pair.compare`:
    `compare` : `([Color], [Color]) -> Order`
      via `Array.compare`, `compare` : `(Color, Color) -> Order` — not found
    `compare` : `([Int], [Int]) -> Order`
      via `Array.compare`, `compare` : `(Int, Int) -> Order` — try importing mo:core/Int
