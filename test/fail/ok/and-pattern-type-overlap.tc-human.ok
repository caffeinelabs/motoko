error[M0260]: type identifier `T` bound in both branches of and-pattern
    ┌─ and-pattern-type-overlap.mo:1:5
  1 │  let { type T } and { type T } : module { type T = Nat } =
    │      ^^^^^^^^^^^^^^^^^^^^^^^^^ 
