error[M0275]: unexpected token '-', expected a block `{ ... }`: `switch` and `do` always take a block, and so do the branches or body of `if`/`while`/`for` when the condition or head is written without parentheses (parenthesize it to keep bare branches)
    ┌─ syntax-head-prefix-op.mo:4:11
  4 │  if inc(1) -1 > 0 { };
    │            ^ 
