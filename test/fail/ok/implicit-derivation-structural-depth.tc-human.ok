error[M0230]: Cannot determine implicit argument `enc` of type
  {inner : {x : Text}} -> Tag
    ┌─ implicit-derivation-structural-depth.mo:17:8
 17 │  ignore encode({ inner = { x = "hi" } });
    │         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ 
    = note: If you're trying to omit an implicit argument named `enc` you need to have a matching declaration named `enc` in scope.
    = note: Implicit derivation failed:
                 depth limit reached (increase with `--implicit-derivation-depth`, current: 1)
