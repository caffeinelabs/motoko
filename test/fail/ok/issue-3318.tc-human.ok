error[M0096]: expression of type
  (x : (Nat, Nat)) -> (Int, Int)
cannot produce expected type
  ((Nat, Nat)) -> ((Int, Int))
because the type 
  Int
 is not compatible with type 
  (Int, Int)
 in results
    ┌─ issue-3318.mo:7:24
  7 │  h<(Nat,Nat),(Int,Int)>(g); // accept
    │                         ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  (Nat, Nat) -> ((Nat, Nat))
because the type 
  Nat
 is not compatible with type 
  (Nat, Nat)
 in results
    ┌─ issue-3318.mo:18:3
 18 │           : ((Nat, Nat)) -> (Nat, Nat) = f; // reject
    │    ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  ((Nat, Nat)) -> (Nat, Nat)
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:21:3
 21 │           : ((Nat, Nat)) -> ((Nat, Nat)) = f; // reject
    │    ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  ((Nat, Nat)) -> ((Nat, Nat))
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:24:3
 24 │           : (Nat, Nat) -> (Nat, Nat) = f; // reject
    │    ^ 
error[M0096]: expression of type
  (Nat, Nat) -> ((Nat, Nat))
cannot produce expected type
  (Nat, Nat) -> (Nat, Nat)
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in results
    ┌─ issue-3318.mo:26:38
 26 │  func t5(f: (Nat, Nat) -> ((Nat, Nat)))
    │                                       ^ 
error[M0096]: expression of type
  (Nat, Nat) -> ((Nat, Nat))
cannot produce expected type
  ((Nat, Nat)) -> ((Nat, Nat))
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:36:1
 36 │           : ((Nat, Nat)) -> ((Nat, Nat)) = f; // accept
    │  ^ 
