error[M0096]: expression of type
  (x : (Nat, Nat)) -> (Int, Int)
cannot produce expected type
  ((Nat, Nat)) -> ((Int, Int))
because the type 
  Int
 is not compatible with type 
  (Int, Int)
 in results
    ┌─ issue-3318.mo:5:27
  5 │  h<(Nat, Nat), (Int, Int)>(f); // reject
    │                            ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  (Nat, Nat) -> ((Nat, Nat))
because the type 
  Nat
 is not compatible with type 
  (Nat, Nat)
 in results
    ┌─ issue-3318.mo:13:41
 13 │           : (Nat, Nat) -> ((Nat, Nat)) = f; // reject
    │                                          ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  ((Nat, Nat)) -> (Nat, Nat)
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:16:41
 16 │           : ((Nat, Nat)) -> (Nat, Nat) = f; // reject
    │                                          ^ 
error[M0096]: expression of type
  (Nat, Nat) -> (Nat, Nat)
cannot produce expected type
  ((Nat, Nat)) -> ((Nat, Nat))
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:19:43
 19 │           : ((Nat, Nat)) -> ((Nat, Nat)) = f; // reject
    │                                            ^ 
error[M0096]: expression of type
  (Nat, Nat) -> ((Nat, Nat))
cannot produce expected type
  (Nat, Nat) -> (Nat, Nat)
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in results
    ┌─ issue-3318.mo:22:39
 22 │           : (Nat, Nat) -> (Nat, Nat) = f; // reject
    │                                        ^ 
error[M0096]: expression of type
  (Nat, Nat) -> ((Nat, Nat))
cannot produce expected type
  ((Nat, Nat)) -> ((Nat, Nat))
because the type 
  (Nat, Nat)
 is not compatible with type 
  Nat
 in arguments
    ┌─ issue-3318.mo:31:43
 31 │           : ((Nat, Nat)) -> ((Nat, Nat)) = f; // reject
    │                                            ^ 
