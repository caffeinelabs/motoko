syntax-head-tight-index.mo:4.25-4.29: syntax error [M0275], unexpected token 'else', expected a block `{ ... }`: `switch` and `do` always take a block, and so do the branches or body of `if`/`while`/`for` when the condition or head is written without parentheses (parenthesize it to keep bare branches)
